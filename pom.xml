<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.biggg-guardian</groupId>
    <artifactId>guardian-parent</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>

    <name>Guardian</name>
    <description>轻量级 Spring Boot API 请求层防护框架（防重提交 + 接口限流 + 接口幂等 + 参数自动Trim + 慢接口检测 + 请求链路追踪 + IP黑白名单 + 防重放攻击 + 接口开关 + 参数签名 + 请求加解密）</description>
    <url>https://github.com/BigGG-Guardian/guardian</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>scj</id>
            <name>sunchengjie</name>
            <url>https://github.com/BigGG-Guardian</url>
        </developer>
    </developers>

    <scm>
        <url>https://github.com/BigGG-Guardian/guardian</url>
        <connection>scm:git:https://github.com/BigGG-Guardian/guardian.git</connection>
        <developerConnection>scm:git:git@github.com:BigGG-Guardian/guardian.git</developerConnection>
        <tag>v${project.version}</tag>
    </scm>

    <issueManagement>
        <system>GitHub Issues</system>
        <url>https://github.com/BigGG-Guardian/guardian/issues</url>
    </issueManagement>

    <!--  参数管理  -->
    <properties>
        <revision>2.0.0</revision>
        <java.version>17</java.version>
        <spring-boot.version>3.5.15</spring-boot.version>
<!--        <servlet.version>4.0.1</servlet.version>-->
<!--        <lombok.version>1.18.30</lombok.version>-->
        <bcprov.version>1.84</bcprov.version>
        <rocketmq.version>2.3.6</rocketmq.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <!--  模块管理  -->
    <modules>
        <!--    核心模块    -->
        <module>guardian-core</module>
        <!--    redis模块    -->
        <module>guardian-storage-redis</module>
        <!--    防重复提交模块    -->
        <module>guardian-repeat-submit</module>
        <!--    接口限流模块    -->
        <module>guardian-rate-limit</module>
        <!--    接口幂等模块    -->
        <module>guardian-idempotent</module>
        <!--    请求参数自动trim模块    -->
        <module>guardian-auto-trim</module>
        <!--    慢接口检测模块    -->
        <module>guardian-slow-api</module>
        <!--    请求链路模块    -->
        <module>guardian-trace</module>
        <!--    IP黑白名单模块    -->
        <module>guardian-ip-filter</module>
        <!--    防重放攻击模块    -->
        <module>guardian-anti-replay</module>
        <!--    接口开关模块    -->
        <module>guardian-api-switch</module>
        <!--    参数签名模块    -->
        <module>guardian-sign</module>
        <!--    请求加密解密模块    -->
        <module>guardian-encrypt</module>
        <!--    整合引用所有Starter模块    -->
        <module>guardian-starter-all</module>
        <!--    示例模块    -->
<!--        <module>guardian-example</module>-->
        <module>guardian-dependencies</module>
    </modules>

    <!--  依赖管理  -->
    <dependencyManagement>
        <dependencies>
            <!-- 导入Spring Boot的官方依赖管理，锁定Spring生态组件的版本 -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Servlet -->
<!--            <dependency>-->
<!--                <groupId>jakarta.servlet</groupId>-->
<!--                <artifactId>jakarta.servlet-api</artifactId>-->
<!--                <version>${servlet.version}</version>-->
<!--            </dependency>-->
            <!-- Lombok（provided：仅编译期使用，不传递给消费方） -->
<!--            <dependency>-->
<!--                <groupId>org.projectlombok</groupId>-->
<!--                <artifactId>lombok</artifactId>-->
<!--                <version>${lombok.version}</version>-->
<!--                <scope>provided</scope>-->
<!--            </dependency>-->
            <!-- 国密算法 -->
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>${bcprov.version}</version>
                <scope>provided</scope>
            </dependency>
            <!-- 自定义模块-核心模块 -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-core</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-Redis存储模块 -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-storage-redis</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-防重复提交核心模块 -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-repeat-submit-core</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-接口限流核心模块 -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-rate-limit-core</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-接口幂等核心模块 -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-idempotent-core</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-Guardian 防重复提交Spring Boot Starter -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-repeat-submit-spring-boot-starter</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-Guardian 接口限流Spring Boot Starter -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-rate-limit-spring-boot-starter</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-Guardian 接口幂等Spring Boot Starter -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-idempotent-spring-boot-starter</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-Guardian 请求链路 Spring Boot Starter -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-trace-spring-boot-starter</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-Guardian 慢接口检测 Spring Boot Starter -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-slow-api-spring-boot-starter</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-Guardian 请求参数自动trim Spring Boot Starter -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-auto-trim-spring-boot-starter</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-IP黑白名单核心模块 -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-ip-filter-core</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-Guardian IP黑白名单 Spring Boot Starter -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-ip-filter-spring-boot-starter</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-Guardian 防重放攻击 Spring Boot Starter -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-anti-replay-spring-boot-starter</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-Guardian 接口开关 Spring Boot Starter -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-api-switch-spring-boot-starter</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-Guardian 参数签名 Spring Boot Starter -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-sign-spring-boot-starter</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-Guardian 请求加密解密 Spring Boot Starter -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-encrypt-spring-boot-starter</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-请求链路核心模块 -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-trace-core</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-请求链路 RabbitMQ 模块 -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-trace-rabbitmq</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-请求链路 Kafka 模块 -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-trace-kafka</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-请求链路 RocketMQ 模块 -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-trace-rocketmq</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-防重放攻击核心模块 -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-anti-replay-core</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- RocketMQ Spring Boot Starter -->
            <dependency>
                <groupId>org.apache.rocketmq</groupId>
                <artifactId>rocketmq-spring-boot-starter</artifactId>
                <version>${rocketmq.version}</version>
            </dependency>
            <!-- 自定义模块-整合引用所有Starter模块 -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-starter-all</artifactId>
                <version>${revision}</version>
            </dependency>
            <!-- 自定义模块-管理所有Starter模块版本模块 -->
            <dependency>
                <groupId>io.github.biggg-guardian</groupId>
                <artifactId>guardian-dependencies</artifactId>
                <version>${revision}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!--  打包配置  -->
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.11.0</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <!-- 统一版本号：将 ${revision} 替换为实际版本，确保发布的 pom 中不含变量 -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <version>1.6.0</version>
                <configuration>
                    <updatePomFile>true</updatePomFile>
                    <flattenMode>resolveCiFriendliesOnly</flattenMode>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- 打包源码 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.0</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- 生成 Javadoc -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.6.3</version>
                <configuration>
                    <encoding>UTF-8</encoding>
                    <charset>UTF-8</charset>
                    <docencoding>UTF-8</docencoding>
                    <doclint>none</doclint>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!--  发布 profile：mvn deploy -P release  -->
    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <!-- GPG 签名（Maven Central 强制要求） -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <!-- Maven Central 发布插件（新版 Central Portal） -->
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.10.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>true</autoPublish>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
